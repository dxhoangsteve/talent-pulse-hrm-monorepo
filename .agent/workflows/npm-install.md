---
description: Install npm packages in FrontEnd folder
---

// turbo-all

1. Navigate to FrontEnd folder and run npm install with any required packages
2. Run `npm install` to install dependencies
3. Run `npm start` or `expo start` to start the development server
